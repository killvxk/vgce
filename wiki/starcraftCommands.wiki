#summary Starcraft commands list used in replays and packets
#labels Game-Starcraft,Type-List,Platform-PC

=Notes=
This list has been merged due to the similarity between Replay data and Packet data.

The command opcode is not stored in the Command ID of the packet, but rather in the data, where many commands may stack.
Command class 1 opcodes are typically those from ~0x37 to ~0x50.


=Command Opcodes=

----------------------------------------------------
==*0x5B - * Make Game Public==
{{{
   // No parameters.
}}}
CLASS 1 ONLY

----------------------------------------------------
==*0x5A - *Merge Dark Archon==
{{{
   // No parameters.
}}}
CLASS 2 ONLY

----------------------------------------------------
*0x59 - *INVALID
----------------------------------------------------
==*0x58 - *Minimap Ping==
{{{
       s16  // x
       s16  // y
}}}
CLASS 2 ONLY

----------------------------------------------------
==*0x57 - *Leave Game==
{{{
	u8  // 1=Player Quit, 6=Player Dropped
}}}
CLASS 2 ONLY

----------------------------------------------------
==*0x56 - *Change Replay Speed==
{{{
    u8   // 0=Play, 1=Pause
    u32  // Game Speed
    u32  // Speed Multiplier
}}}
CLASS 2 ONLY

----------------------------------------------------
==*0x55 - *Set Latency==
{{{
    u8  // Latency setting
}}}
CLASS 2 ONLY

----------------------------------------------------
==*0x54 - * Mission Briefing Start==
{{{
    // No Parameters
}}}
CLASS 1

----------------------------------------------------
*0x53 - * DEFAULT CASE 2 (unknown command) INVALID
*0x52 - * DEFAULT CASE (unknown command) INVALID
*0x51 - *INVALID
----------------------------------------------------
*0x50 - *Unknown
{{{
      // No Parameters.
}}}
INVALID

----------------------------------------------------
==*0x4F - *Map data transfer==
{{{
      u16  // Packet size [after value]
      u16  // Transfer ID
	
	//*ID 0 - Unknown*
		u8[6]   // Unknown
	//*ID 1 - File Name*
		u32       // File size?
		u32       // File hash?
		cstring   // File name
	//*ID 4 - Transfer data*
	      u8   // Unknown)
	      u32  // File offset ?
	      u16  // Block size [after value]
		// ..data..
	//*ID 5 - Request data*
	      u8   // Unknown
	      u32  // File offset ?
}}}
INVALID

----------------------------------------------------
==*0x4E - *Ban Player==
{{{
      u8   // Player ID
}}}
INVALID

----------------------------------------------------
*0x4D - * 		(unban player?)
{{{

}}}
INVALID

----------------------------------------------------
==*0x4C - *Lobby Chat==
{{{
      cstring   // Message
}}}
INVALID

----------------------------------------------------
==*0x4B - *Force Name Transfer==
{{{
      u8[120]   // Unknown
}}}
INVALID

----------------------------------------------------
==*0x4A - *Player/Force Data Transfer==
{{{
      u16  // Game Speed ?
      u16  // Unknown
      u16  // Unknown

      u8   // Player 1 Controller // not accurate?
      u8   // Player 2 Controller
      u8   // Player 3 Controller
      u8   // Player 4 Controller
      u8   // Player 5 Controller
      u8   // Player 6 Controller
      u8   // Player 7 Controller
      u8   // Player 8 Controller
      u8   // Player 9 Controller
      u8   // Player 10 Controller
      u8   // Player 11 Controller
      u8   // Player 12 Controller

      u8   // Player 1 Race
      u8   // Player 2 Race
      u8   // Player 3 Race
      u8   // Player 4 Race
      u8   // Player 5 Race
      u8   // Player 6 Race
      u8   // Player 7 Race
      u8   // Player 8 Race
      u8   // Player 9 Race
      u8   // Player 10 Race
      u8   // Player 11 Race
      u8   // Player 12 Race

      u8   // Player 1 Race // not accurate?
      u8   // Player 2 Race
      u8   // Player 3 Race
      u8   // Player 4 Race
      u8   // Player 5 Race
      u8   // Player 6 Race
      u8   // Player 7 Race
      u8   // Player 8 Race

      u8   // Player 1 Controller
      u8   // Player 2 Controller
      u8   // Player 3 Controller
      u8   // Player 4 Controller
      u8   // Player 5 Controller
      u8   // Player 6 Controller
      u8   // Player 7 Controller
      u8   // Player 8 Controller
      u8   // Player 9 Controller
      u8   // Player 10 Controller
      u8   // Player 11 Controller
      u8   // Player 12 Controller

      u8   // Player 1 owning Force
      u8   // Player 2 owning Force
      u8   // Player 3 owning Force
      u8   // Player 4 owning Force
      u8   // Player 5 owning Force
      u8   // Player 6 owning Force
      u8   // Player 7 owning Force
      u8   // Player 8 owning Force
      u8   // Player 9 owning Force
      u8   // Player 10 owning Force
      u8   // Player 11 owning Force
      u8   // Player 12 owning Force

      8 bytes // Unknown
}}}
INVALID

----------------------------------------------------
==*0x49 - *Information Request==
{{{
      u32   // Information Byte; 6 = File Name, 7 = File Data ??
}}}
INVALID

----------------------------------------------------
*0x48 - * Unknown
{{{
     u32  // Unknown
     u8   // Unknown [player related]
     u8   // Unknown [player related]
     u8   // Unknown [player related]
     u8   // Unknown [player related]
     u8   // Unknown [player related]
     u8   // Unknown [player related]
     u8   // Unknown [player related]
     u8   // Unknown [player related]
}}}
INVALID CLASS 2

----------------------------------------------------
*0x47 - *INVALID
*0x46 - *INVALID
----------------------------------------------------
==*0x45 - *Swap Players==
{{{
	u8  // Slot # Source
	u8  // Slot # Destination
}}}
CLASS 1 ONLY

----------------------------------------------------
==*0x44 - * Slot Change==
{{{
        u8  // Slot #
        u8  // Owner? (Non-standard)
}}}
CLASS 1 ONLY

----------------------------------------------------
*0x43 - *UNKNOWN
{{{
	u8  // Unknown
}}}
CLASS 1 ONLY

----------------------------------------------------
*0x42 - *UNKNOWN
{{{
	u8  // Unknown
}}}
CLASS 1 ONLY

----------------------------------------------------
==*0x41 - *Race Change==
{{{
	u8  // slot #
	u8  // Race ID
}}}
CLASS 1 ONLY

----------------------------------------------------
*0x40 - *Unknown
{{{
     u8[17]   // Unknown
}}}
CLASS 1 ONLY

----------------------------------------------------
*0x3F - *Unknown (Confirmation?)
{{{
	u8   // player ID?
	u16  // Unknown (Null)
	u16  // Unknown
	u16  // Unknown
}}}
CLASS 1 ONLY

----------------------------------------------------
==*0x3E - *Game Slot Modification==
{{{
	u8  // Slot #
	u8  // Player ID
	u8  // Player Owner ID
	u8  // Race ID
	u8  // Force ID
}}}
CLASS 1 ONLY

----------------------------------------------------
==*0x3D - *Map Download %==
{{{
	s8  // Percentage
}}}
CLASS 1 ONLY

----------------------------------------------------
==*0x3C - *Start Game==
{{{
     // No parameters.
}}}
CLASS 1 ONLY

----------------------------------------------------
*0x3B - *Unknown
{{{
	u8  // Unknown
}}}
CLASS 1 & 2

----------------------------------------------------
*0x3A - *Unknown
{{{
	u8  // Unknown
}}}
CLASS 1 & 2

----------------------------------------------------
*0x39 - *Unknown
{{{
   // No parameters.
}}}
CLASS 2 ONLY

----------------------------------------------------
*0x38 - *Unknown
{{{
   // No parameters.
}}}
CLASS 2 ONLY

----------------------------------------------------
==*0x37 - *Game Hash Check==
(determines syncronized gameplay, sent every ~0.2 seconds or before any order)
{{{
   u8[6]   // Unknown
}}}
CLASS 2 ONLY

----------------------------------------------------
==*0x36 - *Stim==
{{{
  //  No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x35 - *Building Morph==
{{{
	u16  // Unit ID (Units.dat)
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x34 - *Cancel Addon==
{{{
  //  No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x33 - *Cancel Upgrade==
{{{
  //  No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x32 - *Upgrade==
{{{
	u8   // Upgrade ID (Upgrades.dat)
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x31 - *Cancel Research==
{{{
  //  No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x30 - *Research==
{{{
	u8   // Tech ID (Techdata.dat)
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x2F - *Lift==
{{{
	s16 // (x) ?
	s16 // (y) ?
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x2E - *Cancel Nuke==
{{{
   // No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x2D - *Unburrow==
{{{
	u8   // Type (unused)
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x2C - *Burrow==
{{{
	u8   // Type (unused)
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x2B - *Hold Position==
{{{
	u8   // Type
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x2A - *Merge Archon==
{{{
   // No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x29 - *Unload==
{{{
	u16  // (Unit ID? [memory])
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x28 - *Unload All==
{{{
	u8  // Type
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x27 - *Build Subunit(Interceptor/Scarab)==
{{{
   // No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x26 - *Siege==
{{{
	u8   // Type (unused)
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x25 - *Unsiege==
{{{
	u8   // Type (unused)
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
*0x24 - * INVALID
----------------------------------------------------
==*0x23 - *Unit Morph==
{{{
	u16  // Unit ID (Units.dat);
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x22 - *Decloak==
{{{
	u8   // Type (unused)
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x21 - *Cloak==
{{{
	u8   // Type (unused)
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x20 - *Cancel Train==
{{{
	u16  // Unit ID (Units.dat);
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x1F - *Train==
{{{
	u16  // Unit ID (Units.dat);
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x1E - *Return Cargo==
{{{
	u8   // Type
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
*0x1D - *"Nothing" ?
{{{
    // No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x1C - *Reaver Stop==
{{{
     // No parameters
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x1B - *Carrier Stop==
{{{
     // No parameters
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x1A - *Stop==
{{{
	u8   // Type
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x19 - *Cancel Unit Morph==
{{{
  //  No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x18 - *Cancel Construction==
{{{
   // No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
*0x17 - *INVALID
*0x16 - *INVALID
----------------------------------------------------
==*0x15 - *Target Command==
{{{
	s16  // x
	s16  // y
	u16  // unit ID (memory) (visible)
	u16  // unit ID (Units.dat) (only if memory ID is unused(0), otherwise set to none)
	u8   // Order ID (Orders.dat)
	u8   // Type
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x14 - *Right Click==
{{{
	s16  // (x);
	s16  // (y);
	u16  // (Unit ID[memory]);
	u16  // (unit ID[Units.dat entry listing]) (only if memory ID is unused(0), otherwise set to none)
	u8   // Type
		0 = // instant;
		1 = // shiftqueue;
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x13 - *Hotkey==
{{{
	u8  //command
		0 = // save;
		1 = // recall;
	u8  // slot
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x12 - *Use Cheat==
{{{
    u32  //(Cheat code flags).
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x11 - *Resume Game==
{{{
    // No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x10 - *Pause Game==
{{{
    // No parameters.
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x0F - *Change Game Speed==
{{{
      u8   //speed
            0="Slowest";
            6="Fastest";
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x0E - *Ally==
{{{
	u2 	// (Alliance p1) [0 = enemy, 1= allied, 2 = allied vic]
	u2 	// (Alliance p2) [0 = enemy, 1= allied, 2 = allied vic]
	u2 	// (Alliance p3) [0 = enemy, 1= allied, 2 = allied vic]
	u2 	// (Alliance p4) [0 = enemy, 1= allied, 2 = allied vic]

	u2 	// (Alliance p5) [0 = enemy, 1= allied, 2 = allied vic]
	u2 	// (Alliance p6) [0 = enemy, 1= allied, 2 = allied vic]
	u2 	// (Alliance p7) [0 = enemy, 1= allied, 2 = allied vic]
	u2 	// (Alliance p8) [0 = enemy, 1= allied, 2 = allied vic]

	u2 	// (Alliance p9) [0 = enemy, 1= allied, 2 = allied vic]
	u2 	// (Alliance p10) [0 = enemy, 1= allied, 2 = allied vic]
	u2 	// (Alliance p11) [0 = enemy, 1= allied, 2 = allied vic]
	u2 	// (Alliance p12) [0 = enemy, 1= allied, 2 = allied vic]

	u8	// Unknown/unused
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x0D - *Vision==
{{{
	0x80 00 // Vision for player 1
	0x40 00 // Vision for player 2
	0x20 00 // Vision for player 3
	0x10 00 // Vision for player 4
	0x08 00 // Vision for player 5
	0x04 00 // Vision for player 6
	0x02 00 // Vision for player 7
	0x01 00 // Vision for player 8
	0x00 80 // Vision for player 9
	0x00 40 // Vision for player 10
	0x00 20 // Vision for player 11
	0x00 10 // Vision for player 12
	0x00 08 // 
	0x00 04 // 
	0x00 02 // 
	0x00 01 // 
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x0C - *Build==
{{{
	u8   // (Command [Orders.dat Entry Listing])
	s16  // (x [tile])
	s16  // (y [tile])
	u16  // (Unit ID [Units.dat entry listing])
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x0B - *Shift Deselect==
{{{
	u8   // (#of Units)
	u16  // (Unit ID[memory])
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x0A - *Shift Select==
{{{
	u8   // (#of Units)
	u16  // (Unit ID[memory])
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x09 - *Select==
{{{
	u8   // (#of Units)
	u16  // (Unit ID[memory])
}}}
CLASS 2 ONLY AND REPLAY

----------------------------------------------------
==*0x08 - *Restart Game==
{{{
      // No parameters.
}}}
CLASS 2 ONLY

----------------------------------------------------
==*0x07 - * Load Game==
{{{
      u32     //(unknown)
      cstring // Name
}}}
CLASS 2 ONLY

----------------------------------------------------
==*0x06 - *Save Game==
{{{
      u32     //(unknown)
      cstring // Name
}}}
CLASS 2 ONLY

----------------------------------------------------
==*0x05 - * Keep Alive==
{{{
      // No parameters.
}}}
CLASS 1 & 2

----------------------------------------------------
*0x04 - *UNDEFINED
----------------------------------------------------
*0x03 - * Unknown
{{{
      // No parameters.
}}}
UNDEFINED
----------------------------------------------------
*0x02 - *UNDEFINED
----------------------------------------------------
*0x01 - * Claim Host ?
{{{
      //No parameters.
}}}
UNDEFINED
----------------------------------------------------
==*0x00 - *Game Chat==
{{{
      u8      // (unknown)
      cstring // (message)
}}}

----------------------------------------------------


=Packet Header=
{{{
	u32  // udptype (0x00)
	u16  // checksum
	u16  // packet length
	u16  // Sent packets to player (seq1)
	u16  // Recieved packets from player (seq2)
	u8   // Command Class
	u8   // Command ID (0x00)
	u8   // Player ID
	u8   // Resend
	*    // ...Packet Data...
}}}
The command opcode is not stored in the Command ID of the packet, but rather in the data, where many commands may stack.

Command class 2 opcodes are those you'd find in a replay; unit actions.

Command class 1 opcodes are others used by Starcraft for communication; send text; lobby data; join game.

=Replay Header=
{{{
   u32  // time (Game Ticks * 255??)
   u8   // unk (Game Ticks??)

   u8   // Player ID
   u8   // Command ID
   *    // ...Command Variables...
}}}